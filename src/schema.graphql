type Query {
  info: String!
  feed: [User!]!
  me: User
  protected: User
}

type Mutation {
  signup(
    email: String!
    password: String!
    firstname: String!
    lastname: String!
  ): Boolean!
  login(email: String!, password: String!): AuthPayload
  logout: Boolean!
  revokeRefreshTokensForUser(userId: Int!): Boolean!
}

type User {
  id: ID!
  firstname: String!
  lastname: String!
  email: String!
}

type AuthPayload {
  accessToken: String
  user: User
}
